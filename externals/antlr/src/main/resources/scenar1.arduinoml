application simpleAlarm

# Declaring bricks
sensor button: 8
actuator led: 12
actuator buzzer: 11

# Declaring states
on {
    buzzer <= HIGH
     led <= HIGH

    button is LOW => off
}

-> off {
    buzzer <= LOW
    led <= LOW

    button is HIGH => on
}