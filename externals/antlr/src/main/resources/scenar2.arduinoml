application dualCheckAlarm

# Declaring bricks
sensor buttonOne: 8
sensor buttonTwo: 9
actuator buzzer: 11
actuator led: 12

# Declaring states
onBuzzerLed {
    buzzer <= HIGH
    led <= HIGH
    buttonOne is HIGH OR buttonTwo is HIGH => offBuzzerLed

}

-> offBuzzerLed {
    buzzer <= LOW
    led <= LOW
    buttonOne is LOW AND buttonTwo is LOW => onBuzzerLed
}