application dualCheckAlarm

# Declaring bricks
sensor button: 8
sensor buttontwo: 9
actuator buzzer: 11

# Declaring states
on {
    buzzer <= LOW
    buttontwo is HIGH OR button is HIGH => off

    A OR B AND C OR D => E

    A => E
    B AND C => E
    D => E
}

-> off {
    buzzer <= LOW
    button is HIGH AND buttontwo is HIGH => on
}